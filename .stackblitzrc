{
  "installDependencies": false,
  "startCommand": "(cd DelOroApp && npm install && npm start) && (cd server && npm install && npx nodemon server)",
  "env": {
    "NODE_ENV": "development"
  }
}
